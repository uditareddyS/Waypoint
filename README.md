"# Street Vendor Digitalization Agent" 
## Current status (Sept 18)
- POST /vendor — live, saves vendor profile to DynamoDB
- POST /vendor/generate — live, returns stubbed scheme/UPI/promo data (real Bedrock+OpenSearch synthesis in progress)
